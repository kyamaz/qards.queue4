# GEMINI.md

このファイルは、このリポジトリのコードを扱う際に、Gemini (gemini.google.com) にガイダンスを提供します。

## プロジェクト概要

これは、日本のゲーム「七並べ」をベースにした、量子コンピューティングに関する教育的なカードゲーム、Quantum Gate Card Game (Qards) です。Next.js 15、React 19、TypeScript、Tailwind CSS v4で構築されています。

## 主要なコマンド

### 開発
```bash
npm run dev      # Turbopackで開発サーバーを起動
npm run build    # プロダクション用にビルド
npm run start    # プロダクションサーバーを起動
npm run lint     # ESLintを実行
```

### テスト
```bash
npm test         # Jestテスト（単体・統合テスト）を実行
npm run e2e      # Cypress E2Eテストをインタラクティブに実行
npm run e2e:headless  # Cypress E2Eテストをヘッドレスで実行
npm run cypress:open  # Cypressテストランナーを開く（開発サーバーが実行中である必要があります）
npm run cypress:run   # Cypressテストをヘッドレスで実行（開発サーバーが実行中である必要があります）
```

## アーキテクチャ概要

### ディレクトリ構造
- `/src/app/` - Next.js App Routerのページとレイアウト
- `/src/components/` - 'use client'ディレクティブを持つReactコンポーネント
  - `GameScreen.tsx` - メインゲームコンテナ
  - `GameBoard.tsx` - ゲームボードの表示
  - `PlayerHand.tsx` - プレイヤーの手札管理
- `/src/game/` - コアゲームロジック（純粋関数、UI依存なし）
  - `types.ts` - すべてのTypeScriptの型とインターフェース
  - `gameLogic.ts` - ゲーム状態管理とルール
- `/test/` - `/src/`構造をミラーリングしたJest単体テスト
- `/cypress/` - Cypress E2Eテスト
  - `/cypress/e2e/` - エンドツーエンドのテストシナリオ
  - `/cypress/fixtures/` - テストデータとモックデータ
  - `/cypress/support/` - カスタムコマンドとユーティリティ

### 主要なアーキテクチャ決定事項

1. **関心の分離**: ゲームロジック (`/src/game/`) はUIコンポーネント (`/src/components/`) から完全に分離されています。これにより、Reactとは独立してゲームルールをテストできます。

2. **型安全性**: すべてのゲームエンティティ（Player, Card, GameState）は `types.ts` で強く型付���されています。ゲームでは、カードの種類に判別共用体（NumberCard | QuantumCard）を使用しています。

3. **状態管理**: ゲームの状態は、useStateフックを使用してReactコンポーネントで管理されます。外部の状態管理ライブラリは使用していません。

4. **コンポーネントパターン**: コンポーネントは 'use client' ディレクティブを使用し、フックを持つ関数コンポーネントです。

## ゲーム特有のコンテキスト

### ゲーム要素 (デッキ合計60枚)
- **初期状態カード (4枚)**: ゲーム開始時に他のカードと共に配布
  - `|0⟩`: 3枚
  - `|1⟩`: 1枚
- **Qubitカード (7枚)**
  - `|+⟩`: 2枚
  - `|-⟩`: 2枚
  - `|0⟩`: 2枚
  - `|1⟩`: 1枚
- **Gateカード (32枚)**
  - `I` (恒等): 8枚
  - `X` (パウリX): 8枚
  - `Z` (パウリZ): 8枚
  - `H` (アダマール): 8枚
- **Unitaryカード (2枚)**
  - `U`: 2枚
- **Controlカード (4枚)**
  - `C`: 4枚
- **Measurementカード (11枚)**
  - `⟨0|`: 4枚
  - `⟨1|`: 3枚
  - `⟨+|`: 2枚
  - `⟨-|`: 2枚
- **Targetカード (動的に生成)**
  - `O`: Controlカード配置時に自動生成（デッキには含まれない）
- **プレイヤー**: 3〜6人。4回パスしたプレイヤーは脱落��るシステム。
- **ボード**: 4レーンの量子回路ボード
- **勝利条件**: ゲーム終了時の測定ポイントから手札のペナルティを引いた点数に基づく
- **脱落システム**: 4回パスしたプレイヤーは個別に脱落し、ゲームは残りのプレイヤーで続行されます。

### TARGETカードの表示
TARGETカードは、「T」ではなく「O」という記号で表示されます。これは「任意の操作（any Operation）」を表します。この設計は、TARGETカードが制御ゲートのターゲットとして任意の量子操作を適用できる位置を表すことを反映しており、「T」よりも「O」の方が直感的で意味的に正しい表現であるためです。

### コアゲームフロー
1.  **初期プレイヤー選択プロセス**:
    1.  **カード配布**: 初期量子ビットカードを含む全カードをプレイヤーにランダムに配布します。
    2.  **手札確認**: 各プレイヤーは手札に初期量子ビットカードがあるか確認します。
    3.  **順次配置**: プレイヤーAから順に、初期量子ビットカードを持つプレイヤーは、手札の**すべて**の初期量子ビットカードをボードの最も左の空きスロットに配置します。
    4.  **先手決���**: `|1⟩`の初期量子ビットカードを配置したプレイヤーが最初のプレイヤーとなります。複数いる場合はターン順が早い方が優先されます。誰も持っていない場合は、最初に初期量子ビットカードを置いたプレイヤーが先手です。
2.  **ターン進行**: `isValidPlay()` でカードの配置を検証しながら、`gameState.currentPlayerId` を通じてターンを管理します。
3.  **プレイヤー脱落**: 4回パスするとプレイヤーが脱落します。
4.  **ゲーム終了条件**: 以下のいずれかの条件が満たされるとゲームが終了します。
    *   いずれかのプレイヤーの手札が全てなくなった時。
    *   測定回数が11回に達した時。
    *   アクティブな（脱落していない）プレイヤーが1人以下になった時。
    *   全てのアクティブプレイヤーが3回以上パスした時。

### カード配置ルール
- **Qubitカード**: 測定カードの後にのみ配置可能。
- **Gateカード**: 測定カードの後ろ以外の任意の位置に配置可能。UnitaryカードやTargetカードの上に重ねることができます。
- **Unitaryカード**:
    - 測定カードの後ろには配置できません。
    - 同じプレイヤーは1ターンに1���しかプレイできません（追加ターンも同じターンと見なされます）。
- **Measurementカード**: 他の測定カード以外の任意のカードの後に配置できます。
- **Controlカード**:
    - 制御位置とターゲット位置は、同じ場所の隣接レーンでなければなりません。
    - 両方のスロットが空である必要があります。
    - 両方のレーンで、先行するすべてのマスにカードが配置されていなければなりません（ギャップは許可されません）。
    - Hゲートは制御のターゲットにはなれません。
- **Targetカード**:
    - Controlカードが配置されると自動的に配置されます。
    - ターゲットレーンの先行カードが測定カードの場合、配置できません。

### スコアリングシステム
- **測定カード**: 測定カードがプレイされると、量子測定の結果に基づいてポイントが与えられます。
  - **測定結果 '1'**: +5ポイント
  - **測定結果 '0'**: +3ポイント
- **手札ペナルティシステム**: ゲーム終了時に手札に残っているカードはペナルティを適用します。
  - **量子ゲートカード (I, X, Z, H)**: 5枚ごとに-2ポイント（切り上げ）
    - 例: 1-5枚 = -2点, 6-10枚 = -4点, 11-15枚 = -6点
  - **その他すべてのカード (Qubit, Measurement, Unitary, Control, Target)**: 1枚ごとに-2ポイント
    - 例: 1枚 = -2点, 3枚 = -6点, 5枚 = -10点
- **最終スコア**: 測定ポイントから手札のペナルティを引いたものが勝者を決定します。

### 量子計算エンジン
ゲームは、複素振幅を用いた実際の量子力学シミュレーションによって量子状態の進化を追跡します。測定時には、量子状態に基づいた確率的な結果をシミュレートします。
- **フォールバック**: 量子計算が利用できない場合、以下の古典的な互換性マトリックスに基づいたスコア計算にフォールバックします。
| Qubit | ⟨0\| | ⟨1\| | ⟨+\| | ⟨-\| |
|---|---|---|---|---|
| \|0⟩ | 3 | 0 | 1 | 1 |
| \|1⟩ | 0 | 3 | 1 | 1 |
| \|+⟩ | 1 | 1 | 3 | 0 |
| \|-⟩ | 1 | 1 | 0 | 3 |
- **デバッグ情報**: 測定時、詳細な量子計算のステップがブラウザのコンソールに記録され、測定結果メッセージには🔬インジケータが表示されます。

## UI/UX

### 設定画面の機能
- **音量調節**: サウンド/音楽 (0-100%)
- **難易度**: 簡単/普通/難しい
- **アニメーション速度**: 遅い/普通/速い
- **ヒント表��**: ON/OFF
- **言語**: 日本語/英語
- **プレイヤー人数**: 3-6人
- **設定の永続化**: 設定はLocalStorageに保存されます。

### ヒントシステム
- **有効な配置ゾーン**: 選択したカードを配置可能な位置がハイライトされます。
- **測定スコア予測**: 測定カードを選択すると、得られるスコアのヒントが表示されます。
- この機能は設定画面でON/OFFを切り替えられます。

### カードのカラーコード
- **Qubit**: 緑 (`bg-green-600`)
- **Initial Qubit**: 濃い緑 (`bg-green-800`)
- **Gate**: 青 (`bg-blue-600`)
- **Unitary**: 紫 (`bg-purple-600`)
- **Control**: 黄色 (`bg-yellow-600`)
- **Measurement**: 赤 (`bg-red-600`)
- **ホバー効果**: `scale-105`
- **選択状態**: `cyan-400`のリング

## 開発ガイドライン

1. **TypeScript**: 厳密な型チェックを使用します。`/src/game/types.ts` から型をインポートしてください。
2. **テスト**: 
   - 新しいゲームロジックについては、`/test/game/` に単体テストを記述してください。
   - コンポーネントテストは `/test/components/` に記述してください。
   - E2Eテストは `/cypress/e2e/` にCypressを使用して記述してください。
3. **パスのインポート**: インポートには `@/` エイリアスを使用してください（`./src/` にマッピングされます）。
4. **クライアントコンポーネント**: インタラクティブなコンポーネントには 'use client' ディレクティブを追加してください。
5. **スタイリング**: すべてのスタイリングにTailwind CSS v4クラスを使用してください。
6. **E2Eテスト**: CypressでのE2Eテストで要素をターゲットしやすくするために `data-*` 属性を使用してください。
   - すべてのインタラクティブな要素には、一意の `data-testid` 属性を持たせるべきです。
   - `data-testid` の値には意味のある名前を使用してください（例：`data-testid="start-game-button"`）。
   - テストのセレクタとしてテキストコンテンツやCSSクラスを使用しないでください。
   - これにより、UIのテキストやスタイリングが変更されてもテストが安定します。

### Gitブランチ戦略
- **featureブランチ** → **developブランチ** → **mainブランチ**
- 新機能やバグ修正のために `develop` からfeatureブランチを作成します。
- プルリクエストを介してfeatureブランチを `develop` にマージします。
- リリース時には `develop` を `main` にマージします。
- `main` ブランチには、安定した本番環境対応のコードが含まれます。

### コミットガイドライン
- **コミット前に必ずテストを実行する**: `npm test` を実行して、すべてのテストがパスすることを確認してください。
- **壊れたコードは絶対にコミットしない**: 変更が既存の機能を壊していないことを確認してください。
- **コミット前のチェックリスト**:
  1. `npm test` を実行 - すべてのテストがパスすること
  2. `npm run lint` を実行 - lintエラーを修正すること
  3. `npm run build` を実行 - ビルドが成功すること
  4. `git diff` で変更内容を確認すること

## ライセンスと著作権

このプロジェクトはMITライセンスの下でライセンスされています。すべてのソースコードファイルには、コンプライアンスと明確性のためにSPDXライセンス識別子が含まれています。

### SPDXライセンス識別子ポリシー
- **すべてのソースファイル** (.ts, .tsx, .js, .jsx, .css) には、両方のSPDXヘッダーを含める必要があります。
- **ヘッダー形式**: 
  ```
  // SPDX-License-Identifier: MIT
  // SPDX-FileCopyrightText: Copyright 2025 OpenQL Project
  ```
  CSSファイ���の場合は `//` の代わりに `/* */` コメントスタイルを使用してください。
- **配置**: SPDXヘッダーは、各ファイルの最初の行でなければなりません。
- **JSONファイル**: コメントをサポートしていないため、JSONファイルはスキップしてください。
- **生成されたファイル**: `next-env.d.ts` のような自動生成ファイルはスキップしてください。

## 国際化 (i18n)

### 実装アプローチ
このプロジェクトでは、外部ライブラリの代わりにReact Context APIを使用した軽量なカスタムi18n実装を使用しています。このアプローチは以下の理由で選択されました：
- 翻訳範囲が限定的（UI要素とゲーム用語のみ）
- 2言語（日本語と英語）のみのサポート
- 動的な翻訳要件のない静的コンテンツ
- パフォーマンス向上のための最小限の依存関係

### i18n構造
```
src/i18n/
├── translations/
│   ├── ja.json    # 日本語翻訳（デフォルト）
│   └── en.json    # 英語翻訳
├── context.tsx    # 言語コンテキストの定義
├── provider.tsx   # 言語プロバイダーコンポーネント
└── useTranslation.ts  # 翻訳用のカスタムフック
```

### コンポーネントでの使用法
```typescript
import { useTranslation } from '@/i18n/useTranslation';

function Component() {
  const { t } = useTranslation();
  return <h1>{t('common.gameTitle')}</h1>;
}
```

### 翻訳キーの構造
- `common.*` - 共通のUI要素（ボタン、タイトル）
- `game.*` - ゲーム固有の用語（ターン、スコアなど）
- `cards.*` - カードタイプの名前と説明
- `settings.*` - 設定画面のラベル
- `rules.*` - ゲームのルールと説明
- `messages.*` - システムメッセージと通知

### 重要事項
- 量子記法（|0⟩, |1⟩など）は言語間で変更されません。
- 数値の書式は言語間で一貫しています。
- 言語設定はlocalStorageに保存されます。
- 言語の変更はページのリロードなしで即座に適用されます。
